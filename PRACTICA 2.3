
#include <xc.h>
#include "REGISTROS.h"
#define _XTAL_FREQ 32000000
#include "flex_lcd.h"
#include <stdlib.h>
#include <stdio.h>

void main(void) {
    
    unsigned char i,j;
    unsigned char buffer1[16];
    
    OSCFRQbits.HFFRQ=0b110;
    ANSELD=0;
    ANSELE=0;
    
    Lcd_Init();
    Lcd_Cmd(LCD_CLEAR);
    Lcd_Cmd(LCD_BLINK_CURSOR_ON);
    __delay_ms(1000);
    

    while(1){
         
        
    for (i=0; i<12; i++) {
    Lcd_Out(1,i+2,"MECATRONICA");
    __delay_ms(500);
    Lcd_Cmd(LCD_CLEAR);
            sprintf(buffer1,"contador %03d",i);
            Lcd_Out2(2,2,buffer1);   
        }    
    }
    
    return;
}
